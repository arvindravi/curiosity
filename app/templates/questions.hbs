{{content-header params=headerParams}}
<section class="section">
  <div class="container">
    <div class="content">
      {{#each model as |q|}}
        {{question-listing question=q}}
      {{/each}}
    </div>
  </div>
</section>
